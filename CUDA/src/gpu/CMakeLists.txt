SET (GPU_GPUNUFFT_SOURCES ${GPUNUFFT_SOURCES} 
		 ${CMAKE_CURRENT_SOURCE_DIR}/precomp_kernels.cu)

ADD_SUBDIRECTORY(atomic)

if(GEN_MEX_FILES)
  ADD_SUBDIRECTORY(matlab)
endif(GEN_MEX_FILES)

if (GEN_PYTHON_FILES)
	ADD_SUBDIRECTORY(python)
endif(GEN_PYTHON_FILES)
